面试技术提问



1. Python 中的 `*args` 和 `**kwargs` 在装饰器（无论是函数还是类）中为什么非常重要？
   
    在编写装饰器时，我们往往不知道被装饰的函数具体接收什么参数。使用 `*args` 和 `**kwargs` 可以让装饰器的包装函数接收任意数量的位置参数和关键字参数，并原封不动地传递给原函数，保证了装饰器的通用性。

2. 在 Python 中，`with` 语句的作用是什么？它背后依赖的是什么协议？`
   
   with语句用于简化资源管理（如文件读写、锁），确保代码块执行完毕后资源能被正确释放（即使发生异常）。它依赖“上下文管理协议”，即对象必须实现 `__enter__` 和 `__exit__` 两个魔法方法

3. 请简述日常开发中 Git 的核心操作流程。
   
   日常开发的标准流程是：从 `main` 分支拉取最新代码并创建功能分支（`feature/xxx`）进行开发；开发完成后提交代码并推送到远程仓库；随后发起合并请求（Pull Request / Merge Request），经过同事代码审查（Code Review）通过后，再合并入 `main` 分支

4. 请简述 Python 中多线程的实现方式，并说明 Lock（线程锁）的作用及基本使用方法？
   
   Python通过`threading`模块实现多线程，常用`Thread`类创建并调用`start()`启动，`join()`等待结束。`Lock`用于解决数据竞争，确保同一时刻仅一个线程访问共享资源。推荐使用`with lock:`语法自动管理锁的获取与释放，防止异常死锁

5. 请简述 Python 类装饰器的实现原理，它与函数装饰器有什么区别？ 
   类装饰器利用类的 `__init__` 方法接收被装饰的函数，利用 `__call__` 方法让实例变成可调用对象。每次调用原函数时，实际上是在执行 `__call__`。区别在于类装饰器可以利用实例属性来保存状态如缓存、计数，而普通函数装饰器通常不保存状态。

6. Python 项目中 `__init__.py` 文件的作用是什么？它的存在告诉 Python 解释器该目录应被视为一个包含模块的包（Package），从而允许使用 `import` 语句导入该目录下的模块。在 Python 3.3+ 中虽然有了隐式命名空间包，但保留该文件仍是最佳实践，且可用于编写包的初始化代码。

7. 在类装饰器中，如何实现“带参数”的装饰器（例如 `@Logger(level="INFO")`）？ 
   带参数的类装饰器实际上是“类的实例”作为装饰器。`__init__` 接收装饰器的参数（如 level），而 `__call__` 接收被装饰的函数（func），并在 `__call__` 内部定义并返回一个包装函数（wrapper）来执行原函数。

8. 请解释 Python 中 `async` 和 `await` 的作用，以及它们如何解决 I/O 阻塞问题？ 
   `async def` 定义一个协程函数，调用它不会立即执行，而是返回一个协程对象。`await` 用于挂起当前协程，让出控制权给事件循环，去执行其他任务，直到等待的异步操作（如 `asyncio.sleep` 或网络请求）完成。这解决了同步代码中（如 `time.sleep`）因等待 I/O 而导致整个程序卡住的问题。

9. `asyncio.gather()` 的作用是什么？如果不使用它，而是依次 `await` 多个异步任务，会有什么不同？ 
   `asyncio.gather()` 用于并发地运行多个协程任务。如果依次 `await task1()` 然后 `await task2()`，程序会等 task1 完成后才开始 task2相当于串行，耗时累加；而使用 `gather`，它们会同时开始并发执行，耗时取决于最慢的那个，从而大幅节省总时间。

10. python 中的多线程和异步编程有什么区别？各自适用于什么场景？

11. 多线程通过操作系统调度多个线程并发执行，在 I/O 等待时会释放 GIL（全局解释器锁）让其他线程运行，但受限于 GIL，无法利用多核 CPU 进行真正的并行计算。异步编程（如 asyncio）基于单线程的事件循环和协程，在遇到 I/O 操作时主动挂起当前任务去执行其他协程，避免了线程切换的开销。多线程适合 I/O 密集型且并发量不极高的场景；异步编程适合高并发 I/O 密集型场景（如网络请求、爬虫）。若是 CPU 密集型任务，则应使用多进程（multiprocessing）来绕过 GIL 限制
